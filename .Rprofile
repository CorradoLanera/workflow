source("renv/activate.R")

if (interactive()) {
    suppressPackageStartupMessages(suppressWarnings({
        library(devtools)
        library(lintr)
        library(spelling)
        library(testthat)
        library(usethis)
    }))
}
